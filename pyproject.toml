[project]
name = "webcam-cloud"
version = "0.1.0"
description = "Webcam stream analytics project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "ruff>=0.11.9",
]
